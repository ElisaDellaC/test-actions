name: push-to-master
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
      
jobs:
  my_first_job:
     runs-on: ubuntu-latest
     steps: 
      - run: echo github.event.pull_request.user.login
      - run: echo ${{ github.event.pull_request.user.login }}
      - run: exit 1
  
  my_second_job:
     runs-on: ubuntu-latest
     steps: 
      - run: npm install
